import React from "react";

type props = {
  className?: string;
};

export default function RelatedProductCarousel({ className }: props) {
  return <div className={className}>RelatedProductCarousel</div>;
}
